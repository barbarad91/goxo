import { createMuiTheme } from '@material-ui/core'

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#d75a4a',
      light: '#eeb3d8',
      dark: '#8a5678',
    },
    secondary: {
      main: '#444444',
      light: '#fff',
      dark: '#a5a5bd',
    },
  },
})

export default theme
